name: Build for CI

on:
  push:
    branches:
      - github-workflow-ci
  pull_request:
    branches:
      - github-workflow-ci

jobs:
  build-f33-x64:
    runs-on: fedora-latest
    name: A job to test building on Fedora 33
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Do the build
        uses: ./.github/build-f33
        id: build
#      - name: Archive production artifacts
#        uses: actions/upload-artifact@v2
#        with:
#          name: shim
#          path: |
#            /destdir
